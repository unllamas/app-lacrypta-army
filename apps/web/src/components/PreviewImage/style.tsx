import { styled } from 'styled-components';

export const PreviewImageStyle = styled.div`
  position: relative;

  width: 100%;

  > button {
    position: absolute;
    top: 12px;
    right: 12px;
  }
`;
